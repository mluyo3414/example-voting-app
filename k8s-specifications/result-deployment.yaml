apiVersion: apps/v1
kind: Deployment
metadata:
  name: result
spec:
  replicas: 1
  selector:
    matchLabels:
      app: result
  template:
    metadata:
      labels:
        app: result
    spec:
      containers:
      - image: mluyo3414/example-voting-app-result:{{CF_BRANCH_TAG_NORMALIZED}}-${{CF_SHORT_REVISION}}
        name: result

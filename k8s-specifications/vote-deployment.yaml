apiVersion: apps/v1
kind: Deployment
metadata:
  name: vote
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote
  template:
    metadata:
      labels:
        app: vote
    spec:
      containers:
      - image: mluyo3414/example-voting-vote:{{CF_BRANCH_TAG_NORMALIZED}}-${{CF_SHORT_REVISION}}
        name: vote

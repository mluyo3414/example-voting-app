version: '1.0'
steps:
  main_clone:
    title: Cloning main repository...
    type: git-clone
    repo: '${{CF_REPO_OWNER}}/${{CF_REPO_NAME}}'
    revision: '${{CF_REVISION}}'
    git: mluyo3414
  LintHelmChart:
    title: Linting Helm Chart
    deploy:
      type: helm
      helm_version: 3.0.1
      working_directory: ./example-voting-app
      arguments:
        action: auth
        kube_context: gke_sandbox-271314_us-central1-c_cluster-1
        commands:
          - helm lint

